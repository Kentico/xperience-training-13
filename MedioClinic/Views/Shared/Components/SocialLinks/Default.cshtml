@model IEnumerable<Business.Models.SocialLink>
@inject CMS.Core.ILocalizationService LocalizationService

<h5 class="white-text">@LocalizationService.GetString("ViewComponent.SocialLink.FollowUs")</h5>
<p>
    @if (Model?.Any() == true)
    {
        foreach (var socialLink in Model)
        {
            if (!string.IsNullOrEmpty(socialLink.IconServerPath))
            {
                <a href="@Html.Raw(socialLink.Url)">
                    <img src="@Url.Kentico().ImageUrl(socialLink.IconServerPath, SizeConstraint.Empty)" alt="@Html.DisplayFor(model => socialLink.Name)" />
                </a>
            }
        }
    }
</p>